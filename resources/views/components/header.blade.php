<section class="w-full flex justify-between">
    <h2 class="text-4xl font-extrabold dark:text-white">
        {{ $title }}
    </h2>
    {{-- @if(isset($link))
        <a href="{{ $link }}" class="mt-2 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">Adicionar Novo</a>
    @endif
    <p class="my-4 text-lg text-gray-500">
        {{ $slot }}
    </p> --}}
</section>
